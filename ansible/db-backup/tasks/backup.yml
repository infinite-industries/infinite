---
# Do a backup
- name: create a backup
  tags: backup
  become: true
  systemd:
    name: infinite-db-backup
    state: started
- name: echo backup info
  tags: backup
  debug:
    msg: The database backup is available at ~/backups/{{ pgdatabase }}.latest
